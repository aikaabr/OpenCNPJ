# Exemplo de configuração para execução totalmente local
# Copie este arquivo para .env e ajuste conforme necessário

# ===========================================
# CONFIGURAÇÃO PARA EXECUÇÃO LOCAL APENAS
# ===========================================

# Storage Configuration - MODO LOCAL
STORAGE_TYPE=filesystem        # Usar FileSystemExporter (local)
STORAGE_ENABLED=true          # Manter habilitado para funcionar localmente

# Alternatively, to disable storage completely:
# STORAGE_ENABLED=false       # Desabilita qualquer tipo de storage/upload

# Local FileSystem Settings
FILESYSTEM_OUTPUT_PATH=./output  # Diretório onde os arquivos serão salvos localmente

# ===========================================
# CONFIGURAÇÕES PARA STORAGE REMOTO (OPCIONAL)
# ===========================================
# Descomente e configure apenas se quiser usar storage remoto

# Rclone Configuration (if STORAGE_TYPE=rclone)
# RCLONE_REMOTE=
# RCLONE_TRANSFERS=100
# RCLONE_MAX_CONCURRENT=4

# S3 Configuration (if STORAGE_TYPE=s3)
# S3_BUCKET_NAME=
# S3_REGION=us-east-1
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
# S3_PREFIX=

# ===========================================
# CONFIGURAÇÕES GERAIS DO SISTEMA
# ===========================================

# DuckDB Configuration
DUCKDB_IN_MEMORY=true
DUCKDB_THREADS=0
DUCKDB_MEMORY_LIMIT=5GB
DUCKDB_ENGINE_THREADS=0
DUCKDB_PRESERVE_ORDER=false

# Processing Configuration
NDJSON_BATCH_SIZE=10000
NDJSON_NORMALIZE=false
NDJSON_WRITE_FILES=false
NDJSON_MAX_PARALLEL=8
DOWNLOADER_PARALLEL=6

# Web Interface
WEB_PORT=8080

# Analytics
ANALYTICS_ENABLED=true

# ===========================================
# INSTRUÇÕES DE USO
# ===========================================
#
# Para execução TOTALMENTE LOCAL (sem uploads):
#   1. Use STORAGE_TYPE=filesystem e STORAGE_ENABLED=true
#   2. Execute: ./quick-local.sh p (pipeline local)
#   3. Dados ficam em ./output
#
# Para DESABILITAR qualquer storage:
#   1. Use STORAGE_ENABLED=false
#   2. Execute: ./quick-local.sh no-upload
#   3. Dados ficam apenas nos diretórios de processamento
#
# Para storage REMOTO:
#   1. Configure S3 ou Rclone acima
#   2. Use STORAGE_TYPE=s3 ou STORAGE_TYPE=rclone
#   3. Execute: ./quick.sh p (pipeline com upload)
#